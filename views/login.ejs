<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/login.css" />
    <link rel="stylesheet" href="/styles/bootstrap.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css"
    />
    <script src="/js/JQuery.js"></script>
    <title>Login Page</title>
  </head>
  <body>
    <div class="container-fluid">
      <div
        class="row d-flex justify-content-center align-items-center min-vh-100 p-3"
      >
        <div class="header d-flex align-items-center">
          <i
            class="fas fa-shopping-cart mx-2 my-2 fs-2 text-white"
            style="rotate: -15deg"
          ></i>
          <a href="/" class="navbar-brand fw-bold fs-3"
            ><span class="fw-bold text-white fs-4">Yamazon</span></a
          >
        </div>
        <div class="col-lg-12">
          <form action="/login" method="POST" class="form-log p-3 p-md-5">
            <h3 class="text-center fw-bold text-white">Login</h3>
            <div class="i-group d-flex flex-column">
              <label for="email"><b>Email</b></label>
              <input
                type="email"
                name="email"
                id="email"
                placeholder="Email"
                class="fw-bold"
                autocomplete="on"
                autofocus
              />
              <span
                id="email-err"
                class="text-danger fw-bold"
                style="display: none; font-size: 0.8rem"
              ></span>
              <label for="password"><b>Password</b></label>
              <input
                type="password"
                name="password"
                id="password"
                placeholder="Password"
                class="fw-bold"
              />
              <input
                type="submit"
                value="Login"
                class="btn btn-lg mt-5 rounded-pill"
              />
            </div>
            <p class="text-center mt-3"><b>don't have an account ?</b></p>
            <a href="/register" class="btn btn-lg w-100 fw-bold rounded-pill"
              >Register</a
            >
          </form>
        </div>
      </div>
    </div>
    <div id="Popup" class="login-popup">Please fill all fields</div>
    <script src="/js/function.js"></script>
    <script src="/js/main.js"></script>
  </body>
</html>
